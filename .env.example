#!/usr/bin/env bash
# Example environment variables (copy this file to .env.local and fill with real values)
# SECURITY: Never commit real API keys. The key below must be replaced with a browser-restricted key.

# Google AI API Key for Gemini integration (client-side usage)
# Obtain: https://aistudio.google.com/
# IMPORTANT: Create an API key restricted to HTTP referrers ONLY if exposed to the browser.
NEXT_PUBLIC_GOOGLE_API_KEY=your_public_google_api_key_here

# If you later move API calls server-side, you can add a secure (non-public) key:
# GOOGLE_AI_API_KEY=your_server_side_google_api_key_here